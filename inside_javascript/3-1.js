// 숫자 타입
var intNum = 10;
var floatNum = 10;

// 문자열 타입
var singleQuoteStr = 'single quote string';
var dobuleQuoteStr = "dobule quote string";
var singleChar = 'a';

// 불린 타입
var boolVar = true;

// undefined 타입
var emptyVar;

// nul 타입
var nullVar = null;

console.log(typeof intNum, typeof floatNum, typeof singleQuoteStr, typeof dobuleQuoteStr, typeof boolVar, typeof nullVar, typeof emptyVar);